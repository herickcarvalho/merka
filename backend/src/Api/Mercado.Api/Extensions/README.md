# Extensions

Extension methods transversais do host (ex.: `AddApplicationServices`,
`AddSwaggerDocumentation`), usados para manter o `Program.cs` enxuto à
medida que a configuração cresce. Vazio propositalmente por enquanto.
